#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Menu {
    History,
    Deposit,
    Emergency,
    Home,
    Send,
    CreateVaults,
    RevaultVaults,
    DelegateFunds,
    Settings,
    Vaults,
}
